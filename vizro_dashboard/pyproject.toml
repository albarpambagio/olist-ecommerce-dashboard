[project]
name = "olist-vizro-dashboard"
version = "0.1.0"
description = "Olist Sales & Customer Analytics Dashboard"
requires-python = ">=3.10"
dependencies = [
    "vizro",
    "pandas",
    "psycopg2-binary",
    "plotly",
]

